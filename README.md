# Inverted-Search
Developed an Inverted Index system in C using hashing to enable fast and efficient word search across multiple text files. The project stores each word with its file occurrences and count using multi-level linked lists, improving search performance and data organization through structured indexing.
